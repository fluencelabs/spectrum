apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: system-grafana-dashboards
    files:
      - dashboards/kubernetes.json
    options:
      labels:
        grafana_dashboard: "1"
        app.kubernetes.io/part-of: system
        app.kubernetes.io/component: monitoring
