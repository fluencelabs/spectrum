apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
  namespace: kube-system
spec:
  interval: 15m
  chart:
    spec:
      chart: openebs/openebs
      version: 3.12.2
      sourceRef:
        name: openebs
        kind: HelmRepository
        namespace: flux-system
