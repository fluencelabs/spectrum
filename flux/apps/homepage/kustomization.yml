apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helm-repository.yml
  - ./helm-release.yml

configMapGenerator:
  - name: homepage-values
    namespace: default
    files:
      - ./config.yml

configurations:
  - kustomize-config.yml
